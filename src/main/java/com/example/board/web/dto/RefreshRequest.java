package com.example.board.web.dto;

public record RefreshRequest(String refreshToken) {
    // This record holds the refresh token for refreshing JWT tokens.
    // It can be extended with validation annotations if needed.

}
